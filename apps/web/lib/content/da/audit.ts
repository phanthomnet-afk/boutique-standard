import type { AuditPageContent } from "../types";

// Danish translation pending — using English content
import enContent from "../en/audit";

const content: AuditPageContent = enContent;

export default content;
