{
  "name": "boutique-standard",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/web",
    "pdf": "npm run generate --workspace=packages/pdf-engine",
    "typecheck": "tsc --noEmit",
    "checkpoint": "bash scripts/checkpoint.sh",
    "restore": "bash scripts/restore.sh",
    "snapshots": "git tag -l 'checkpoint/*' | sort -r",
    "seed:report": "npx tsx apps/web/scripts/seedReport.ts",
    "db:push": "npm run db:push --workspace=apps/web",
    "db:migrate": "npm run db:migrate --workspace=apps/web",
    "db:generate": "npm run db:generate --workspace=apps/web",
    "db:studio": "npm run db:studio --workspace=apps/web",
    "db:seed": "npm run seed:report --workspace=apps/web"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "allowScripts": {
    "@prisma/client@5.22.0": true,
    "@prisma/engines@5.22.0": true,
    "prisma@5.22.0": true
  }
}
